# Decision log

Format: `- [module] decision — reasoning — alternative considered`

- [infra] Custom Postgres image extending `postgis/postgis:16-3.4` with `postgresql-16-pgvector` from PGDG — no official image ships both PostGIS and pgvector — alternative: two separate databases (rejected: cross-DB joins for parcel-scoped retrieval) or an unofficial combined image (rejected: provenance/trust).
- [infra] Extensions created via `docker-entrypoint-initdb.d` on first boot AND re-asserted `IF NOT EXISTS` in the first Alembic migration — a fresh `make migrate` must be self-sufficient on any empty DB, not only the compose one.
- [infra] Host port mapped `5433:5432` — avoids collision with a developer's local Postgres on 5432.
- [schema] Chunks carry denormalized filter fields (commune_code, language, legal_status, document_date, document_type) — retrieval must filter the space *before* vector/lexical scoring (M3.1) and exclude repealed text with no join (debrief Q9) — alternative: fully normalized, join to `documents` at query time (rejected: join before the vector scan, weaker index filtering).
- [schema] Amendment chains modeled as self-referential FKs `supersedes`/`superseded_by` on `documents` plus a `legal_status` enum — covers bonus +8 without a full version table — alternative: separate `document_versions` table (deferred: heavier than 3 weeks warrants).
- [schema] ELI stored as nullable unique key; primary key stays a UUID — communal PDFs and geodata have no ELI, so ELI cannot be a universal PK despite the spec's suggestion — alternative: ELI-as-PK for Legilux only (rejected: mixed PK strategy across one table).
- [ingestion] Plain typed Python scripts, not Prefect/Dagster/Celery — the corpus is a small serial batch; a scheduler adds ops surface with no payoff at this scale — alternative: Prefect (deferred to the 102-commune scale-out, noted in SCALING.md).
- [schema] `embedding vector(1024)` pinned to a BGE-m3 / multilingual-e5-large class model — multilingual FR/DE/LB coverage, self-hostable so indexing cost is compute not per-token API (debrief Q1) — alternative: OpenAI text-embedding-3-large 3072-d (rejected: per-token cost, larger index, no LB).
- [schema] `legal_status` includes `unknown` as the ingest-time default — honest state for freshly fetched docs whose status is not yet determined, rather than guessing in_force — alternative: default in_force (rejected: violates the no-guessing rule and risks presenting repealed text as current).
- [schema] chunks.tsv is a stored generated tsvector using the `simple` config (no stemming) — preserves article numbers, defined terms and rare tokens, which lexical legal search depends on — alternative: per-language stemmed configs (deferred to M3 if recall needs it).
- [infra] Alembic `include_object` ignores reflected objects absent from ORM metadata — stops autogenerate emitting DROP for PostGIS/tiger-geocoder tables the extension owns — alternative: drop tiger geocoder in init (rejected: brittle across base-image versions).
- [infra] Enum types created once at the top of the migration with `create_type=False` on columns — types shared by documents+chunks would otherwise CREATE TYPE twice and fail — alternative: distinct per-table enum names (rejected: duplicates the vocabulary).
- [M1] Parcels carry TWO commune codes: `cadastral_commune_code` (ACT's own PCN numbering, e.g. `001`) and `admin_commune_code` (LAU2, e.g. `0501`) — verified via `liste-communes-sections.csv` that these are genuinely different numbering systems and NOT 1:1 after historical mergers (cadastral communes 001 "Arsdorf" and its former neighbour are now both sections inside administrative commune "Rambrouch"). Cadastral code drives the commune+section+number reference search (M1.3); admin code drives which commune's PAG/bylaw applies (M2/M3 scoping) — conflating them would misattribute regulations for any merged commune — alternative: single admin-only or cadastral-only code (rejected: breaks one of the two required lookups, see debrief Q6 prep in WALKTHROUGH.md).
- [M1] `parcel_natures` / `building_natures` are reference tables (code, label, category), not Python enums — these are ACT-owned open vocabularies (~50 and ~34 values) that also carry a "category" grouping useful for map styling, unlike the small closed vocabularies (`legal_status` etc.) that justified enums in the provenance schema — alternative: hardcoded Python enum (rejected: duplicates ACT's own taxonomy and can't carry the category grouping).
- [M1] Building↔parcel modeled as a persisted `parcel_buildings` join table (parcel_id, building_id, overlap_m2), computed once at ingest via `ST_Intersects` — verified the real BATIMENTS.dbf has NO parcel FK and NO building ID at all (only `CODE_OCCUPATION` + `CODE_COMMUNE`), so a building's parcel(s) can only ever be resolved spatially, and a building can genuinely span two parcels (graded M1.3 edge case) — alternative: live `ST_Intersects` at query time (rejected: pushes a geometric computation into every side-panel request instead of paying it once at ingest) or a single nullable FK (rejected: silently wrong for the multi-parcel case).
- [M1] Address fuzzy search: `pg_trgm` GIN index on an unaccented/lowercased generated column, plus a small hand-written abbreviation table (`r.`→`rue`, `str.`→`strooss`, etc.) applied in Python at query time, not stored as a DB table — the abbreviation set is fixed and code-reviewable, not runtime data — alternative: trigram alone (rejected: weak on abbreviations, which share almost no trigrams with their expansion) or a full tsvector+trigram hybrid (rejected: more moving parts than one autocomplete field needs).
- [M1] `unaccent()` is not `IMMUTABLE` in stock Postgres, so it cannot be used directly in a generated column or a plain functional index — the migration wraps it in a small `IMMUTABLE` SQL function (`unaccent_immutable`) that pins the `unaccent` dictionary explicitly. Caught by testing the generated column, not anticipated in advance.
- [M1] Declared area (`area_declared_m2` in the M4 report schema) is honestly `null` for now — verified PCN's `PARCELLES.dbf` has exactly 7 fields (`ID_PARCELLE`, `CODE_COMMUNE`, `CODE_SECTION`, `NUMERO_PRINCIPAL`, `NUMERO_SECONDAIRE`, `LIEUDIT`, `CODE_NATURE`) and none of them is an area/superficie value. CACLR's `IMMEUBLE` file is a candidate but is an undocumented fixed-width legacy export not yet decoded — logged as an open thread, not fabricated.
- [M1] Small curated reference vocabularies (parcel/building natures, the commune registry, the cadastral↔administrative crosswalk — a few KB each, hand-verified against source) live in `backend/app/reference_data/`, tracked in git — NOT `backend/app/data/`, which `.gitignore`'s existing `data/` rule (intended for bulk cadastre/imagery dumps) silently swallows. Caught because the seed CSVs didn't show up in `git status` — alternative: a `.gitignore` exception for one subpath (rejected: more fragile than just not reusing an already-overloaded directory name).
- [M1] `section_code` widened from `VARCHAR(2)` to `VARCHAR(3)` in a follow-up migration — triggered by a seed-loader failure, root cause resolved once real parcels were ingested: `liste-communes-sections.csv` uses a DIFFERENT section-code convention (`HaA`, `HoC`, disambiguating across an administrative commune's several cadastral communes) than the real `PARCELLES.dbf` field (`CODE_SECTION`, always a single letter, scoped within one cadastral commune). `cadastral_sections` is therefore NOT joinable to `parcels.section_code` as originally assumed — kept as a standalone reference table; the section-name lookup for the M1.3 side panel is deferred rather than bridged with a guessed string transform.
- [M1] Reference-table seeding uses Postgres `INSERT ... ON CONFLICT DO UPDATE` (upsert) keyed on each table's natural PK/unique constraint, not a truncate-and-reload — re-running `make seed-reference` is a verified no-op (same row counts, confirmed by actually re-running it), matching M2.1's idempotency requirement even though this loader itself sits outside the `sources`/`documents` provenance tracking (it's static ACT/CACLR vocabulary, not a crawled, citable document).
- [infra] Added `app/core/logging.py` (structlog, JSON output) ahead of the first ingestion script that needed to log anything — enforces the "structured logs, not prints" coding standard from the start rather than retrofitting it once several scripts already use `print`.
- [M1] Ingestion downloads use data.public.lu's stable `/r/<uuid>` redirect URLs, not the dated snapshot path (`download.data.public.lu/resources/.../TIMESTAMP/file`) — the dated URL for BD-Adresses returned a live 404 mid-session because the dataset had refreshed since the URL was first captured. Caught by actually re-running the ingester days apart, not anticipated — a real illustration of why M2's crawler needs its own re-resolution logic, not hardcoded URLs, for any "weekly updated" source.
- [M1] Geodata parsing uses pyshp + shapely (pure Python), not GDAL/ogr2ogr — no system package manager step, keeps `make ingest` working with only `pip install -e .[dev]` on a clean machine. Spatial predicates (parcel↔building overlap, address→parcel containment) run as a SQL pass in PostGIS after loading, not in Python — alternative: GDAL (rejected: outside the mandatory stack, adds an undocumented system dependency).
- [M1] Buildings use scoped delete-then-reinsert per ingest run (not upsert) — the real BATIMENTS source has no natural key at all (verified: only `CODE_OCCUPATION` + `CODE_COMMUNE` + geometry), so there is nothing to upsert against. `parcel_buildings` is safe to recompute every run since it cascade-deletes with its buildings.
- [M1] Bulk inserts are batched (2000 rows/statement) — Postgres caps bound parameters at 65535 per statement, and Luxembourg City alone has enough parcels/addresses to exceed that in a single `INSERT ... VALUES`. Caught by actually running the ingester against real Luxembourg City data, not anticipated from Wiltz alone (confirms the "large, complex" profile was the right pick for the contrast).
- [M1] `parcel_buildings` requires `ST_Area(ST_Intersection(...)) > 1.0` m², not `> 0` — `ST_Intersects` is true for a mere shared-boundary touch. Caught on real data: a building appearing to span 9 parcels actually had meaningful overlap (142-420 m²) with only 3; the other 6 were floating-point slivers down to 0.0000012 m². A plain `> 0` filter would have overstated the "building spans multiple parcels" edge case with noise, not real cases.
- [testing] Tests run against the same Postgres `make ingest` populates (real Wiltz + Luxembourg City data), not a separate throwaway DB — the mandatory "e2e test hitting a real parcel" only means something if the parcel is real. Each test gets its own connection wrapped in an outer transaction, rolled back at teardown (verified: real row counts unchanged after a full test run). Tests that need to assert an expected `IntegrityError` use `session.begin_nested()` (a real SAVEPOINT) around just that insert, not the outer transaction — the first attempt called `session.rollback()` directly, which rolled back the *outer* per-test transaction itself (same underlying transaction, since the Session was bound directly to an already-open connection) and produced a `SAWarning: transaction already deassociated` at teardown; harmless here (nothing ever gets committed either way) but not the correct pattern, fixed once actually run.
- [testing] Schema-constraint tests reuse real seeded reference rows (`cadastral_communes`, `parcel_natures`) as FK targets instead of inventing fixture rows — `make seed-reference` is already a prerequisite for the app to do anything, so there is no scenario where those tests could run without it.
- [M1.2] `addresses.street_name_normalized` had NO trigram index at all until caught by actually running `EXPLAIN ANALYZE` on the search query — the design decision was documented (DECISIONS.md) but never implemented in the `Address` model. Without it: 110ms, sequential scan over all 23,680 rows. With `ix_addresses_street_name_trgm` (GIN, `gin_trgm_ops`): 12.6ms, bitmap index scan. A design decision written down is not the same as a design decision built — this is exactly why the M1.2 EXPLAIN ANALYZE requirement exists, and it earned its keep here.
- [M1.2] Address search combines three things, each covering what the others miss: `pg_trgm`'s `%` similarity operator (typos, e.g. "Lng" still matches "Lang"), a small hand-written abbreviation table applied before normalization (`r.`→`rue`, etc.), and a leading-digit split so "1 Rue du Fort Thüngen" filters on house number 1 AND fuzzy-matches the street separately, rather than fuzzy-matching the whole string as one blob. Does NOT solve full FR/DE/LB street-name variants (needs CACLR's `ALIAS.RUE` real alias data, not a synonym table) — logged as a real gap, not silently covered by the abbreviation table.
- [M1.3] `identify_by_point` returns a LIST, never a single parcel — 0 results (click missed every parcel, e.g. a road) and >1 results (click landed exactly on a shared boundary, the explicit graded edge case) are both real, honest outcomes the API surfaces rather than silently resolving by picking one.
- [M1.1] Frontend EPSG:2169 reprojection (proj4, for OpenLayers) uses PostGIS's own `spatial_ref_sys.proj4text` `towgs84` parameters (`-189.6806,18.3463,-42.7695,-0.33746,-3.09264,2.53861,0.4598`), not a plausible-looking guess — a first attempt with different (also plausible-looking) parameters was off by **108 metres** on the same known coordinate pair used for the backend's reprojection proof. Verified before writing any UI code: with PostGIS's exact parameters, proj4 agrees with the backend's PostGIS/PROJ transform to ~1.5mm. This is exactly the kind of error that would silently misidentify a parcel on every map click — caught by testing against the backend's own value, not assumed correct because the numbers "looked reasonable."
- [M1.1] The OpenLayers `View` uses an explicit `resolutions` array (metres/pixel: 200 down to 0.125), not OL's default zoom levels — OL's built-in zoom→resolution mapping assumes a Web-Mercator-like world extent, which is meaningless for a small, custom, meter-based CRS like LUREF. Caught by actually loading the map in a browser (Playwright), not by reading the code: at "zoom 15" with default levels, tile requests came back with a ~2.4m × 2.4m bbox — visibly wrong (grey tiles, nothing rendered). Same root cause as the address-search index gap: a plausible-looking default silently didn't apply to our specific setup, and only running it for real revealed that.
- [M1.1] Base layer names (`Basemap`, `ortho_latest`, `PCN`) and the working WMS endpoint (`wms.geoportail.lu/opendata/service`) were verified with a live `GetMap` request (curl, saved and viewed the PNG) before writing any React code, not assumed from the `GetCapabilities` layer list alone — confirms the endpoint actually serves imagery in EPSG:2169, not just that a layer with that name exists.
- [M1.1] Frontend verification used a real headless browser (Playwright + Chromium, installed for this purpose — no MCP/browser tool was available), not just `tsc`/`eslint`/curl. This is what caught both real bugs above (the datum shift was caught via `node -e` before any UI code existed; the resolution/zoom bug was only visible by actually loading the map and screenshotting it) plus ruled out a false alarm (orthophoto tiles appeared blank at first but were simply still loading — confirmed by waiting longer, not "fixed" by guessing at a cause).
- [infra] `.gitignore` comments must be on their own line, not appended after a pattern — `.node-local/  # comment` is parsed as one literal (and useless) pattern, not a pattern plus a comment. Caught by `git check-ignore` returning exit 1 for a path the file was supposed to exclude, not by reading the file.
- [M1.4] Real thematic overlay layers (PAG, Natura 2000, flood zones, etc.) were found by reading the *official geoportail.lu web client's own* theme config (`map.geoportail.lu/themes?interface=main&all=true`), not `GetCapabilities` — that endpoint alone doesn't surface them. Layers are addressed by a **numeric WMS ID**, not the friendly name shown in that config (`pag_pag` → `LAYERS=698`) — verified live with GetMap + GetFeatureInfo before writing any code. Full mapping + the two categories with no real layer at all (zone verte, HV electricity) in SOURCES.md.
- [M1.4] No WFS exists on `wms.geoportail.lu/public_map_layers/service` (confirmed: `SERVICE=WFS` → an OWS exception), so overlay intersects are computed by point-sampling GetFeatureInfo (parcel centroid + up to 4 boundary points) rather than a true national bulk-ingested polygon-vs-polygon test — a deliberate, user-confirmed trade-off. GetFeatureInfo's `INFO_FORMAT=application/json` unexpectedly returns the full feature **geometry**, not just attributes, so once a sample point *does* hit something, the overlap area is computed exactly via shapely against our own stored parcel geometry — only "did we find the hit at all" is approximate, not the area once found.
- [M1.4] Results are computed once per parcel and cached in `parcel_overlay_results` (unique on parcel_id+layer_code, upserted) — this external WMS is slow (~1s/request) with no server-side cache of its own, so re-querying it on every parcel-detail request would both be slow and repeatedly hit a free public server for data that can't have changed since the last request. First view of a new parcel: ~10s (18 layers × up to 5 sample points, capped at 8 concurrent requests). Every view after: ~25ms, served entirely from our own DB.
- [M1.4] A line/point-geometry overlay feature (or a polygon merely touching the parcel at a boundary, zero measure) can legitimately intersect with `overlap_m2 == 0.0`, e.g. `gas_network`'s "within 250m of a pipeline" buffer touching a parcel's corner — this is reported honestly as a real, if marginal, intersection rather than hidden; `overlap_m2` is set to `None` only when the returned feature's geometry type isn't a polygon at all (area is undefined, not just small).
- [M1.1/M1.4] Base **and** overlay layers use `ImageWMS`/`ol/layer/Image` (one request per view), not `TileWMS`/`ol/layer/Tile` — re-confirmed with real user testing: a real address ("6 Rue d'Allamont") looked visibly "misaligned" against the government's own map. Investigated properly before acting: (1) independently verified our stored coordinate against OpenStreetMap's Nominatim geocoder — agreed to ~1.5m, so the *data* was never wrong; (2) web-searched the symptom and found it's a well-documented WMS/MapServer issue (a real OpenLayers GitHub issue, #9327, describes the identical symptom — label repeat with TileWMS, gone with ImageWMS) caused by each tile being rendered independently with no awareness of its neighbours; MapServer's own fix for this (`tile_metatile_level`) is a *server*-side mapfile setting we don't control, since `wms.geoportail.lu` is a Luxembourg government service, not ours. `ImageWMS` is the documented client-side workaround when the server can't be configured — confirmed visually afterward (same address, same overlays, seams gone, labels intact). A `mapLoading` indicator was re-added (driven by `imageloadstart`/`imageloadend`/`imageloaderror` across every layer's source, keyed so multiple simultaneously-loading overlays don't fight each other) since a single request is slower than many small parallel ones.
- [M1.5] Road frontage and the neighbour list both need "is this parcel actually a road" — user-confirmed to use ACT's own `parcel_natures.category = 'Voie de communication'` taxonomy rather than any external road dataset, since real ingested data already tags ~1,915 parcels this way. Two different subsets, on purpose: frontage counts only vehicular-road natures (autoroute, route nationale, chemin repris, chemin d'exploitation, rue, place voirie — codes 5035/5036/5037/5038/5041/5043), excluding railway/cycle path/footpath, since those aren't road access a building could use; the neighbour list additionally excludes railway/cycle path/footpath too (the full category, codes above plus 5039/5040/5044), since none of those are a "neighbouring property" either, even though they're not "road frontage." Frontage = `ST_Length(ST_CollectionExtract(ST_Intersection(ST_Boundary(parcel), ST_Boundary(road_parcel)), 2))` summed over touching road parcels — `ST_CollectionExtract(..., 2)` pulls out only the line parts, so a boundary intersection that degenerates to a point (a corner touch) contributes 0 length instead of erroring. Verified against real data: a parcel confirmed by hand to touch a road parcel got a real ~24m frontage; a parcel confirmed to be surrounded entirely by other private parcels (20/20 nearest neighbours at distance 0) correctly got 0.0 frontage — an honest "landlocked" result, not a bug.
- [M1.5] Neighbour distance = user-confirmed choice: minimum boundary-to-boundary `ST_Distance` for every non-road parcel within 30m (not just directly-touching parcels), capped at the 20 nearest, so a dense Luxembourg City block can't return an unbounded list. 0.0 is the common case (an actually-touching neighbour); a positive distance surfaces a real gap (a stream, an unclassified strip) between two parcels that aren't adjacent.
- [M1.5] Slope source, researched (not guessed) before writing any code: ACT publishes a real, open (CC0) 2024 LiDAR-derived terrain model on data.public.lu (`BD-L-Lidar2024`) as a single country-wide Cloud-Optimized GeoTIFF (COG), 50cm resolution — verified live to already be in EPSG:2169 (matching our parcels, no reprojection). The file is ~40GB, not something to download; a COG supports HTTP range requests, so GDAL's `/vsicurl/` driver (via `rasterio`) pulls only the pixel window over one parcel's bbox — verified live: ~2-3s for an uncached parcel (real elevation values, e.g. 322-324m for a real Luxembourg City parcel), ~20ms once cached. `GDAL_DISABLE_READDIR_ON_OPEN`/`CPL_VSIL_CURL_ALLOWED_EXTENSIONS` are set in code (not left to deployment config) so this works the same under `make run` as it did in manual testing, without silently depending on an undocumented env var.
- [M1.5] Slope is exposed as its own endpoint (`GET /parcels/{id}/slope`), not folded into the main parcel-detail payload like frontage/neighbours — the latter are sub-millisecond PostGIS queries, but an uncached slope computation is a multi-second remote read; bundling it would make every first-time parcel-detail view pay that cost even for a caller who never looks at slope. Cached in `parcel_slope_results` per parcel, same lazy-compute-once pattern as M1.4 overlays. Computed via `numpy.gradient` on the windowed elevation grid (pixel size 0.5m from the file's own resolution) — this only captures local (per-pixel) terrain slope, not a smoothed/averaged slope over the whole parcel footprint, so `max_slope_pct` can include a real but sharp single-pixel artifact near a building or vegetation edge in the bare-earth DTM (verified live: one real parcel showed avg 12.4%, max 140.5% — the max is a genuine noisy DTM pixel, not a computation bug, and is reported honestly rather than smoothed away). A parcel outside the raster's extent, or entirely covered by nodata pixels, gets an all-null cached result (`sample_pixel_count == 0`) — an honest "no data available", never a fabricated zero.
- [M1.5] Buildable envelope = user-confirmed choice: a single manually-entered uniform setback (metres), applied as `ST_Buffer(parcel.geom, -setback_m)`, not a front/side/rear differentiated model — because PAG/PAP setback values aren't reliably extractable yet (M2, national/commune legislation ingestion, hasn't started), the brief itself explicitly permits exposing this as a manual input instead of fabricating a number, and that's the answer implemented here. A setback large enough to fully erode the parcel returns `is_empty: true` / `geometry_wgs84_geojson: null` (verified live: setback=100m on a real ~6226m² parcel correctly returns empty, not an error or a negative area) — a real, expected outcome for a small parcel with a large setback, distinguished by an explicit flag rather than inferring emptiness from `area == 0` (a genuinely tiny sliver would look the same by that test). `setback_m` is validated `>= 0` at the API layer (a negative value would be an expansion, not a setback, and is rejected with 422).
- [BUG, high severity] **WMS 1.3.0 + EPSG:2169 was silently querying the wrong real-world location, in both the map AND the M1.4 backend.** Reported by the user as "8 Rue Beck" rendering a forest instead of Ville-Haute. Investigated (not patched blind): the address's own stored coordinate independently reverse-geocoded via Nominatim to a real shop at that exact address (data was correct); the government's own Basemap WMS layer, queried at the identical BBOX we were sending, showed "Septfontaines" — a real place ~15km away. Root cause, confirmed against the authoritative EPSG registry (epsg.io/2169: *"Axes: northing, easting (X,Y)"*): EPSG:2169's registered axis order is Northing,Easting, and WMS **1.3.0** is spec-required to honour a CRS's registered axis order in `BBOX` — our code (frontend `ImageWMS` sources AND the backend's hand-rolled `GetFeatureInfo` calls in `app/services/overlays.py`) sent 1.3.0 requests with the natural (Easting,Northing) order, silently landing ~15km off for many/most locations. **Blast radius:** this affected map rendering (base layers, M1.4 overlay layers) AND — much more seriously — every M1.4 `parcel_overlay_results` row computed before the fix, since GetFeatureInfo was querying the wrong location's features. Confirmed live for the "known constrained parcel" from earlier M1.4 testing (054A00242005292): pre-fix it showed "2 of 18" constraints (Findel servitude, gas network); post-fix, re-computed from scratch, it correctly shows **5 of 18** (also PAG zoning, POS perimeters, drinking-water protection zone) — the earlier result wasn't just imprecise, it was materially missing real constraints. **Fix, after presenting 3 researched options and getting explicit user sign-off:** downgrade to WMS **1.1.1** (`SRS` not `CRS`, `X`/`Y` not `I`/`J` for GetFeatureInfo pixel coords) in both places — WMS 1.1.1's BBOX is always Easting,Northing regardless of a CRS's registered axis order, so it sidesteps the ambiguity entirely with zero changes to any BBOX-building code, verified against the live server for both GetMap and GetFeatureInfo before implementing. Rejected alternatives: keep WMS 1.3.0 and declare `axisOrientation: 'neu'` explicitly (more standards-modern, but two independent code paths — OL's projection registration and the backend's raw httpx params — to get right and re-verify, more risk for a fix this consequential); embed the official geoportail.lu map (would have sidestepped the frontend symptom but does nothing for the backend M1.4 bug, and abandons the click-to-identify/highlight/overlay-toggle integration already built). The entire `parcel_overlay_results` cache (828 stale rows) was truncated, not just left to expire — every constraint result now on record was computed after the fix. A regression test (`test_wms_params.py`) asserts the GetFeatureInfo param shape directly (`VERSION=="1.1.1"`, `SRS` present, `CRS`/`I`/`J` absent) so this can't silently drift back.
- [M5, scope] No spec text exists anywhere for M5 ("procedure assistant") — unlike M1.4/M1.5, the brief's exact wording was never provided. User explicitly chose to proceed without it rather than pausing to request it, on the condition that M5 avoid the M2/M3 dependency (no legislation crawler, no embeddings/hybrid retrieval). Scoped down accordingly, and labelled honestly in the UI ("not a chatbot, and not specific to this parcel") rather than oversold as an AI assistant it isn't: a structured, citation-backed display of exactly ONE real government procedure (guichet.public.lu's "Demander une autorisation de bâtir" — the real building-permit procedure, found via web search, not guessed), ingested through the `sources`/`documents`/`chunks` provenance schema that already existed from the M1/M2 foundation (no new tables). This is a smaller, honest slice of what "procedure assistant" likely means, not a claim that M5 is fully built — a real chatbot/multi-procedure version is M3's job once retrieval exists.
- [M5] Real content only, extracted from the live page via BeautifulSoup (`ingestion/procedure_extraction.py`), not paraphrased/hand-typed — every section's text, the exemption threshold (€6,197.34), the validity period (2 years + 1-year extension), and all 5 legal-basis citations (with their real Legilux ELI URLs, e.g. `Loi du 19 juillet 2005`) are the government's own verbatim French text. `document_date` (2014-06-16) was extracted from a bare `DD.MM.YYYY` pattern on the page with no clearly-labelled "last updated" field — an honest best-effort, documented as such rather than presented as a verified field. Idempotent via the existing `documents` schema's `UniqueConstraint(source_url, sha256)`: re-running the ingestion script is a no-op if the page hasn't changed, and would create a new document version (not silently overwrite) if it has — the same amendment-aware design M2 will use for real legislation.
- [M5] Extraction logic tested against a saved snapshot of the real page (`tests/fixtures/guichet_autorisation_batir.html`, ~230KB, committed) rather than a live fetch in every test run — a real government page can change or go down at any time, and the snapshot is what the parser is actually written against; the live fetch path itself (`download_cached`, rate-limited, descriptive User-Agent, matching the M1 ingestion scripts) was run and verified by hand instead.
- [M2, spec] The exact requirement for PAG/PAP zoning retrieval was never in the assessment PDF — it was dictated by the user via three screen-recording transcripts (2026-09-03), demoing the workflow against two communes (Mersch, Lintgen) that are neither of our two ingested target communes (Wiltz, Luxembourg City). Captured verbatim-in-substance in `PAG_PAP_SPEC.md` (a new top-level doc, not folded into DECISIONS.md, since it's a requirements spec — a different kind of artefact) before writing any code, and user-confirmed to keep targeting Wiltz + Luxembourg City rather than switch/add communes.
- [M2] Researched (not assumed) that both target communes publish a real, open (CC0) per-commune PAG dataset on data.public.lu — Luxembourg City (`pag-c026.zip`, ~1.95GB) and Wiltz (`pag-c023.zip`, ~406MB) — containing real INTERLIS/GML vector data (already EPSG:2169) plus the real DOCX/PDF written and graphic parts. Both ZIPs are large but stored uncompressed, so ingestion (`ingestion/remote_zip.py`) reads only the specific entries needed via HTTP range requests rather than downloading the whole archive — verified live: fetching just the ~61MB GML entry out of a 1.95GB zip, and individual ~15-35KB DOCX entries, each as isolated range GETs.
- [M2] The key architectural finding, verified directly against real data before designing anything around it: resolving "which document applies to this parcel" is a standard point-in-polygon spatial join, not the visual leader-line/annotation matching the demo videos showed manually. Real `ZONAGE` polygons carry a `NOM_FICHIER` attribute naming the exact written-part document (verified: a real `MIX_u` polygon's `NOM_FICHIER` is literally `026_PE_MIX_u`, which exists in the same zip); real `ZONES_QE` (PAP "Quartier Existant") polygons carry **both** `NOM_FICHIER_EC` (written) and `NOM_FICHIER_GR` (graphic). So both parts resolve via the same real GIS attribute, not a fragile string-matching or visual lookup. `pag_zones`/`pap_qe_zones` (new tables, `app/models/pag.py`) store `written_document_id` resolved to a real `documents.id` at ingest time — never a filename string re-matched at query time.
- [M2] PAP QE's graphic-part PDFs (`NOM_FICHIER_GR`) are deliberately **not** fetched/ingested as `Document` rows this pass — Luxembourg City's 23 graphic PDFs alone total ~250MB, and none of them are text-extracted yet, so fetching them now would just be bandwidth spent on content nothing reads. `PapQeZone.graphic_document_filename` stores the real filename reference only — an honest, documented gap (not a fabricated citation), not a `Document` FK.
- [M2, coefficients] The video's numeric coefficients (e.g. "0.3, 0.5, 0.6, 20") are real text/table content **inside** the written-part DOCX, not separate GIS attributes — verified by fully extracting `026_PE_MIX_u.docx` (`python-docx`): real legal article text ("Art. 5 Zone mixte urbaine [MIX-u]") plus a real table mapping specific schéma-directeur codes (e.g. `SD GS-12`, matching real `_SD_PE_*.pdf` filenames) to minimum-residential-floor-area percentage overrides. `ingestion/pag_document_extraction.py` extracts paragraphs **and** table rows (cell-joined) into one chunk per document — the same document-ingestion discipline as M5, not a new architecture. `article_ref` (e.g. "Art. 19") is parsed from the document's own first line via a regex tolerant of the source's own inconsistent spacing ("Art. 5" vs "Art.19").
- [BUG] ACT's own per-commune PAG GML exports disagree on coordinate axis order between communes — Luxembourg City's `ZONAGE`/`ZONES_QE` geometry is Easting,Northing (as parsed, matching real parcels immediately); Wiltz's is Northing,Easting (verified live: as-parsed, 0% of Wiltz's 10,873 real parcels intersected any ingested zone; a diagnostic bbox comparison showed the ingested zones' X-range exactly matched real parcels' Y-range and vice versa — a textbook axis swap; swapping fixed it immediately). Rather than hardcode a per-commune exception (unexplained, untested against the next commune), `ingestion/ingest_pag_zones.py::_resolve_axis_swap` checks empirically against that commune's own already-ingested real parcels (M1) — using a sample of parsed polygons, it tries both orientations and adopts whichever actually overlaps real geometry, raising if neither does (a genuine unresolved discrepancy, never silently guessed past). This is the same *class* of bug as the WMS 1.3.0/EPSG:2169 axis-order bug found earlier this session, in a completely different real dataset — confirms it's worth treating "does this axis order actually match real geometry" as a standing verification habit for every new Luxembourg geospatial source, not a one-off fix.
- [DATA GAP, real, not a bug] After fixing the axis-swap bug, PAG zone coverage is genuinely partial in both communes' downloadable bulk datasets: 65.0% of Luxembourg City's real parcels and 53.8% of Wiltz's intersect at least one ingested `ZONAGE` polygon. Confirmed this is a real characteristic of the *downloaded* dataset, not a parsing bug: fetched the live WMS image for one specific gap location (a real Hollerich address, `054A00396005426`) and it clearly shows dense, real zone labels (HAB-1, HAB-2, REC, VERD, BEP) rendered by the *live* geoportail service — meaning the live PAG data has coverage there that this specific downloadable export snapshot doesn't. The GML's own `<Gestion>` metadata block states `<OBJET>refonte</OBJET>` (a PAG recast/revision in progress) for Luxembourg City, consistent with an in-progress migration explaining the gap. Reported honestly: `pag_zones`/`pap_qe_zones` return an empty list for an uncovered parcel (not an error, not a fabricated zone), and the frontend explicitly labels this as "a real, confirmed gap in the current source dataset for some areas, not an error" rather than implying the parcel has no zoning at all.
- [M2] The video's numeric planning coefficients (COS/CUS/CSS/DL) — asked about directly afterward — turned out to be real, queryable GIS attributes on a GML feature class not yet ingested (`NQ_PAP`, "PAP Nouveau Quartier"), not something requiring PDF-table parsing as first assumed for PAP QE's coefficients (see the earlier M2 entry above). Verified live against both communes: `COS_MIN`/`COS_MAX` (Coefficient d'Occupation du Sol — ground footprint ratio), `CUS_MIN`/`CUS_MAX` (Coefficient d'Utilisation du Sol — floor area ratio), `CSS_MAX` only, no `CSS_MIN` in the real data (Coefficient de Scellement du Sol — soil-sealing ratio; there's genuinely no minimum requirement, so the field isn't modelled), and `DL_MIN`/`DL_MAX` (Densité de Logement — dwelling units/hectare) are all real, populated numeric fields, e.g. a real Weimershof zone: COS≤0.6, CUS≤1.25, CSS≤0.8, DL≤115 — matching the video's example numbers almost exactly ("0.3, 0.5, 0.6, 20" style values). New `pap_nq_zones` table, ingested and spatially joined the same way as `pag_zones`/`pap_qe_zones`. The zone's own written regulation (`NOM_FICHIER_EC`, a shared generic `026_PE_PAP_NQ.docx`/`023_PE_PAP_NQ.docx` document, not per-zone) is ingested and cited; the per-project schéma-directeur PDF (`NOM_FICHIER_SD_EC`/`NOM_FICHIER_SD_GR`) is a filename reference only, same deliberate scope limit as PAP QE's graphic parts.
- [BUG] A real parcel can genuinely intersect multiple separately-digitized `ZONAGE` polygons of the *same* category (e.g. several disjoint "FOR" fragments across one large forest parcel) — the frontend's React key for that list (`category-genre`) assumed uniqueness that doesn't hold, causing a real "duplicate key" console warning caught by live Playwright verification, not by inspection. Fixed by keying on array index instead, matching the pattern already used for `pap_qe_zones`/`pap_nq_zones`.
- [M1.4, gap closed] After being asked directly to audit M1 against the assessment's own scored spec text (not memory/summary), found two of M1.4's explicitly-named overlay categories — "zone verte" and "PAP NQ / PAP QE perimeters" — genuinely weren't answerable through the M1.4 overlay mechanism, even though 18 other real layers were. Both turned out to be cheaply closable using M2 data already ingested, not new scope: (1) the real PAG legend (GetLegendGraphic for WMS layer 698, already inspected during M1.4's original research) groups four real `ZONAGE` categories — `AGR`/`FOR`/`PARC`/`VERD` — under a "Zone verte" heading, consistent with the legal definition (Art. 6, loi protection de la nature) noted as an open question in SOURCES.md at the time; (2) M2's `ZONES_QE`/`NQ_PAP` polygon data already answers "does a PAP NQ/QE perimeter intersect this parcel" exactly, distinct from the narrower `pap_approuves` WMS layer (individually *approved* PAP projects only). `app/services/pag_zoning.py::derive_m14_style_constraints` computes both as `OverlayConstraint`-shaped entries (same intersects/detail/source_url shape as the other 18 M1.4 layers) from the already-fetched `PagZoningInfo` — no extra queries — and `get_parcel_detail` appends them to the same `constraints` list, so the existing frontend "Regulatory constraints" UI renders them with zero code changes (verified live: "21 apply" instead of 18, zero console errors). HV electricity easements remain the one genuine, unresolved gap — confirmed no such layer exists on this public service at all, not something derivable from data we have.
- [M2/M3, scope] After being asked directly whether per-parcel document retrieval is "done" for the ~16 non-PAG/PAP M1.4 overlay categories (environnement/archéologie/eau PDFs), audited honestly: only "intersects yes/no" facts exist for these (M1.4's point-sampled WMS results) — no actual regulatory text has been ingested for any of them, so nothing is retrievable yet beyond the boolean. Ran 5 parallel background research agents across all 16 categories to find real, verified (not guessed) government sources for each, and tiered the results by real feasibility rather than assuming uniform difficulty: **Tier 1** — single real document governs the whole layer (PSL/PST/PSZAE/PSP sectoral plans; Flood HQ20/HQ100's PGRI; Findel servitude RGD); **Tier 2** — a real, structured per-instance link already exists in the source data (ZPIN's own `lien_legilux` GIS attribute; Natura 2000's SITECODE, needing one static SITECODE→RGD table); **Tier 3** — real source exists but needs more investigation (ZPS's exact attribute schema; Legilux Art. 35 fetch failed for the research agent's tool, worth retrying with our own httpx/BeautifulSoup); **Tier 4** — genuine unresolved gaps for now (protected buildings, archaeological sites, gas network, POS). User chose to implement Tier 1 first, then Tier 2, in small commits, deferring Tier 3/4 or documenting them as honest gaps.
- [M2, Tier 1] Legilux's `data.legilux.public.lu/filestore/.../jo/fr/html/....html` export uses one consistent "richtext" HTML structure (`div.richtext_body` > `div.richtext_article`) across every RGD/loi checked so far, not something specific to one document — verified live against the real PSL règlement (10 real `Art. N` articles extracted correctly). `ingestion/legilux_extraction.py` is written as a general-purpose extractor on that basis, reused unchanged for PST/PSZAE/PSP. Each real article becomes its own `Chunk` (matching the project's existing per-article PAG/M5 chunking), tagged `DocumentType.reglement_grand_ducal`/`LegalStatus.in_force`. A zero-width space (U+200B) prefixes each article's real text in the live markup — not a parsing artifact — so the article-ref regex match required an explicit `.lstrip("​")`, not just `.strip()`.
- [M2, Tier 1] `app/services/documents.py::get_document_reference` (extracted from what was previously duplicated inline in `pag_zoning.py`) makes an explicit single-chunk-vs-multi-chunk distinction: single-chunk documents (a PAG zone's own written text, M5's procedure) show that chunk's `article_ref`/`text` inline, unchanged from before; multi-chunk documents (a full RGD, 8-16 real articles) show ONLY the document-level `title`/`source_url` — dumping every article inline in a constraint card isn't usable, and the full text stays reachable via the citation link (and will be searchable once M3's retrieval exists). Verified live: PSL/PST/PSZAE/PSP constraints on a real parcel ("8 Rue Beck") all carry a real document citation regardless of whether that layer intersects — the RGD governs the whole sectoral plan, not just the intersecting feature.
- [M2, Tier 1] `OverlayLayer.document_url` (new, optional field in `app/overlay_layers.py`) is set only for layers genuinely governed by a single real document — 4 of 18 so far (PSL/PST/PSZAE/PSP) — left `None` for the rest rather than guessing a plausible-looking URL. `ingestion/ingest_overlay_documents.py` is config-driven off that field (one entry per layer, not one hardcoded ingestion function per document, matching the M1.4 config's own stated design principle), reusing the exact `Source`/`Document`/`Chunk` idempotent-by-`(source_url, sha256)` pattern already established in `ingest_pag_zones.py`/`ingest_procedure_building_permit.py`. `get_parcel_detail` resolves each Tier-1 layer's document with one batched `source_url IN (...)` query, not N per-layer queries.
- [M2, Tier 1] Findel airport servitude RGD found and verified the same way as the earlier session's secondary lead (`amenagement-territoire.public.lu`'s POS "Aéroport et environs" page) said it should be, but that page alone never gave the exact Legilux URL — fetched it directly, found the one `legilux.public.lu/eli/etat/leg/rgd/2006/05/17/n1/jo` link on it, then confirmed the `data.legilux.public.lu` filestore HTML export exists at the same `eli/.../n1/jo/fr/html/...` path pattern PSL/PST/PSZAE/PSP already use (33 real articles extracted, same richtext structure, zero extractor changes needed). Read the actual extracted article text before trusting the match: this RGD's title is about the whole "Aéroport et environs" *POS* (land-use zoning), broader than the `ana_vfr_findel_lim` WMS layer's specific "aviation height limit" scope — but Art. 19-28 are genuinely the height/radio-navigation-servitude articles (radiophares, DME, radar, VHF/UHF — each with its own explicit height-limit clause), confirming this really is the correct legal basis and not just a same-topic-sounding document.
- [M2, Tier 1, re-scoped] The Flood HQ20/HQ100 layer's legal basis was originally assumed (from the earlier research pass) to be one national "PGRI" planning document — checked properly before implementing and found this was wrong: the real regulatory instrument is a **separate règlement grand-ducal per river basin** (e.g. RGD 30 March 2022 a188 "Alzette et Wark"; a187 "Sûre supérieure, Wiltz, Clerve et Our"; a185 "Mamer et Eisch"; a186 "Moselle et Syre" — all declaring their own basin's flood-hazard/risk maps mandatory), structurally identical to Tier 2's per-instance pattern, not Tier 1's one-document-per-layer pattern. Confirmed by fetching `eau.gouvernement.lu`'s own legislation page directly and reading its real link list, not by trusting an AI-summarized fetch of the same page — **which had already gotten this wrong once**: an earlier `WebFetch`-summarized read of that exact page claimed a185 was the Wiltz RGD, but the real fetched a185 document's own extracted title is "Mamer et Eisch"; the real Wiltz RGD is a187. Lesson generalised: for anything load-bearing (a specific document number, not just a topic), verify against the actual fetched/extracted content, never an AI tool's summary of a page, even when the summary sounds confident.
- [M2, Tier 1, re-scoped] Presented this finding to the user as a real design-weight decision (per the project's own standing rule) rather than silently reinterpreting Tier 1's definition — user chose a static `admin_commune_code -> document_url` mapping (`OverlayLayer.document_url_by_commune`, new field alongside `document_url`) over (a) continuing to chase a watercourse-name GIS attribute for genuine per-feature linking, or (b) deferring flood-zone citations as an honest gap. Verified against the real government legislation page for our exact two target communes only: `"0304"` (Luxembourg City, Alzette) -> a188, `"0807"` (Wiltz) -> a187. Explicitly does not scale to Luxembourg's other ~100 communes without more entries — an honest, already-tracked limitation (see the M2 102-commune scaling analysis in PROGRESS.md's plan), not a hidden one. Tried first to get a genuine per-feature attribute out of the live WMS (`GetFeatureInfo` on layer 3262/HQ100): confirmed via a real fetched GetMap screenshot that a flood polygon does render at a known real Alzette location, then grid-searched `GetFeatureInfo` around and inside that exact area — zero hits every time, while the identical technique reliably returns a real feature for a different layer (Findel servitude) at a real coordinate. Left unresolved whether that's a genuine raster-only/non-queryable quirk specific to this layer or a fixable request-shape issue — not pursued further since the commune-level mapping already gives a correct, verified answer for both communes in scope.
- [M2, Tier 1] `ingest_overlay_documents.py`'s `ingest()` dedupes by `document_url` before fetching/ingesting (`flood_hq20` and `flood_hq100` intentionally share the exact same `document_url_by_commune` dict, since one RGD declares both the HQ20 and HQ100 maps mandatory together) — verified live: the real ingestion run fetched each of the two flood RGDs exactly once, not twice, confirmed by the log showing one `download.fetch` per unique URL rather than one per layer referencing it.
- [BUG, frontend] Caught while walking the user through live testing: `ConstraintsSection` only ever rendered a Tier-1 document citation inside the amber "applies" cards (`intersects === true`) — but the backend deliberately attaches these citations *regardless* of `intersects` (a sectoral-plan RGD governs the whole plan, not just the parcels currently zoned under it — see the Tier-1 decisions above). For the real test parcel used throughout this work ("8 Rue Beck"), PSL/PST/PSZAE/PSP all have `intersects: false`, so the citation was silently invisible — buried in the collapsed "N checked and not applicable" list, which rendered only a plain label with no document at all. This made the whole Tier-1 feature look like it wasn't working for the exact parcel it had been verified against via curl. Fixed by rendering the citation in the "not applicable" list too (muted styling, not amber, plus an explicit "doesn't apply at this specific location" qualifier so it doesn't read as a false positive) — not just moving the same card, since a non-applicable citation needs different framing than an applicable one.
- [M2, Tier 2] Before implementing ZPIN's dynamic linking, checked whether Natura 2000 (the other Tier-2 item) has the same kind of direct per-feature Legilux attribute, rather than assuming the earlier research's "needs a static SITECODE table" conclusion still held — it doesn't: a real Natura 2000 habitats feature (`GetFeatureInfo` on layer 540, live-verified: SITECODE `LU0001018`, "Vallée de la Mamer et de l'Eisch") only carries `SITECODE`/`SITENAME`/`surfha`, no link field. ZPIN (layer 804), checked the same way, genuinely does carry one — a real feature ("Gréngewald", `code_national` "SMF08/PS06") returned `lien_legilux: https://legilux.public.lu/eli/etat/leg/rgd/2024/01/24/a15/jo`. So the two Tier-2 items need genuinely different mechanisms, confirmed by direct verification rather than carried over from the earlier research pass unchecked.
- [M2, Tier 2] Discovered, and confirmed across 8 real documents ingested so far (no exceptions), that the transform from a `legilux.public.lu/eli/.../jo` link to its real `data.legilux.public.lu` richtext HTML export URL is fully mechanical: take everything after `/eli/` in the original link, replace `/` with `-`, and the export lives at `.../filestore/eli/<path>/fr/html/eli-<path-with-dashes>-fr-html.html`. Implemented as `ingestion/legilux_extraction.py::eli_url_to_richtext_html_url` — removes the need to manually hunt down each document's exact filestore URL by hand (as was done for Tier 1's 6 documents), which matters for Tier 2 since ZPIN's real link can point at any of Luxembourg's real reserves, not a small fixed set we could pre-verify one by one.
- [BUG/GAP, M1.3] Discovered while trying to give the user concrete steps to test ZPIN's citation live: `frontend/lib/api.ts::findParcelByReference` (M1.3's cadastral-reference search) was wired up as an API client function and backend-tested since the M1 foundation, but never actually called from any component — no search form existed. This meant the two real parcels needed to demo ZPIN (forest parcels with no street address) were reachable only via the raw API, not the UI. Fixed by adding `components/ReferenceSearch.tsx` (commune/section/numero_principal/numero_secondaire inputs) wired into `app/page.tsx` alongside `AddressSearch`, reusing the exact same 0/1/many-results handling already established for map-click identify (`handleMapClick`) rather than inventing new semantics for a third parcel-selection entry point. Known, accepted limitation: unlike address selection, selecting a parcel by reference doesn't fly the map to it (no lon/lat in `ParcelSummary` to animate to) — the side panel still updates correctly, which is what's needed to see citations; not fixed now since it's a map-UX nicety, not a data-correctness gap.
- [M2, Tier 2, design] Presented ZPIN's implementation as a real architecture choice (per the project's standing rule) rather than deciding unilaterally: static per-reserve mapping (cheap, matches the flood-zone pattern, but only covers reserves manually discovered near our 2 communes) vs. dynamic lazy ingestion (fetches+ingests the real document the first time any real parcel hits a given reserve, generalizing to all ~67 reserves nationally without pre-enumerating them — genuinely needed since there's no WFS to list ZPIN features ahead of time). User chose dynamic. Implemented as `app/services/legilux_dynamic.py::ensure_document_from_eli_link`, mirroring the lazy-compute-once pattern already used for the WMS overlay results themselves (`app/services/overlays.py`) — cached via a new nullable `parcel_overlay_results.document_id` FK (migration `74fe4bc43e92`, verified upgrade/downgrade both apply cleanly), so a second parcel hitting the same reserve resolves in ~1 DB lookup, not a re-fetch (verified live: two real parcels intersecting the same "Gréngewald" reserve — `035B00891000002`/`035B00687000450` — resolved to the identical `document_id`, and `sources.documents_ingested` correctly shows `1`, not 2). Unlike the offline batch ingestion scripts (which raise loudly on failure, by design — an operator should know immediately if a real government page changed shape), this runs inside a live API request: a transient fetch/parse failure is logged and returns `None` rather than raised, so a bonus citation failing never breaks the actual parcel-detail response.
- [M2, Tier 3/4, re-scoped] Went to investigate Tier 3 (ZPS's unclear attribute schema, retrying a failed Legilux Art. 35 fetch) and Tier 4 (heritage/archaeological/gas/POS, originally judged genuine gaps) at the user's explicit request. Couldn't recover enough context on what the "Legilux Art. 35" reference from the original research pass actually was (a background agent's report, no longer available in detail) — reported that honestly rather than guessing at a document to fetch. Checked each remaining layer's real `GetFeatureInfo` attributes directly instead, the same way ZPIN/Natura 2000 were checked, rather than trusting the original tiering as final:
  - **Drinking-water protection zones (ZPS, layer 573)** — genuinely carries a direct per-feature Legilux link, `'Publication du règlement grand-duca'` (the government's own real attribute name is missing its trailing "l" — verified live, not a typo in this codebase). Reuses `document_url_detail_key`/`ensure_document_from_eli_link` unchanged — no new code, just a one-line config addition. Verified live: a real Luxembourg City parcel (`101A00987004029`) resolved the real 16 May 2019 RGD for the "Siweburen"/"Katzebuer-Millebaach" groundwater catchments.
  - **POS perimeters (layer 710)** — also carries a direct link, `LienMemori`. Verified live for the real "Aéroport et environs" perimeter (`054A00380006884`): resolves to the RGD of **19 October 2020**, a later amendment — genuinely different from `findel_servitude`'s statically-cited 17 May 2006 original, since that's a different WMS layer (3212) with its own separate attribute. Both citations are correct for their own layer; not reconciled into one, since they're real, distinct legal references.
  - **Archaeological sites (layer 2560)** — `GetFeatureInfo` returns a hard server-side "internal error" for every request tried, across multiple `INFO_FORMAT`s — confirms the existing `queryable=False` config is correct, not just untested. A genuine, technical dead end, not a research gap.
  - **Gas network (layer 1494)** — real attributes are Creos/SUDenergie operator contact text ("you're within 250m of a main," an email template), never a legal citation of any kind. Confirms this is a genuinely different *kind* of gap than the others — there's no per-feature document to find here at all, not just one that hasn't been found yet.
  - **Heritage/SSMN (layer 709)** — `GetCapabilities` claims `queryable="1"`, but real probing across a dense cluster of visibly-rendered protected buildings in Ville-Haute returned zero features every time (empty JSON, empty HTML, "internal error" on GML) — a real mismatch between advertised and actual server behavior. Confirms the existing `queryable=False` config, already correct.
- [BUG] While verifying the two new dynamic-linked layers above against real ZPIN test parcels, hit a real `sqlalchemy.exc.MissingGreenlet` error — but only in the test suite, never against the live dev server. Root cause: `app/core/db.py`'s real production `SessionLocal` sets `expire_on_commit=False` explicitly, but `tests/conftest.py`'s `async_db_session` fixture built its own `AsyncSession(bind=connection)` without that setting, defaulting to `expire_on_commit=True`. Several services legitimately commit mid-request (`overlays.py`, `slope.py`, now `legilux_dynamic.py`) — with the test fixture's mismatched default, that commit expired every other already-loaded `ParcelOverlayResult` in the session too (not just the one written), and the very next loop iteration's attribute read on an untouched object triggered an unsafe lazy-refresh. First tried working around it in application code (capturing fields into locals before any commit-triggering call) — reverted that once the real root cause was found, since `expire_on_commit=False` makes the workaround unnecessary complexity for a bug that can no longer occur. Fixed with one line in `conftest.py` instead, matching the test session's config to what production actually uses — the right fix aligns the test environment with reality, not patches around a mismatch nobody meant to introduce.
- [M2, Tier 2] Natura 2000's static SITECODE table: found the real per-site RGD list by fetching `environnement.public.lu`'s own Natura 2000 page directly and parsing its real `<tr>` table rows myself (BeautifulSoup, matching SITECODE cells to their real Legilux link) — not by trusting an AI-summarized read of the same page a second time, after the earlier flood-zone episode. 8 real sites found intersecting our two communes (5 habitats via layer 540, 3 birds via layer 533), each site's real RGD fetched and its actual extracted title checked to match the expected site name exactly (e.g. LU0001022 → "Grunewald", confirmed word-for-word) before being added to `_NATURA2000_HABITATS_DOCUMENT_URLS`/`_NATURA2000_BIRDS_DOCUMENT_URLS`. New `OverlayLayer.document_url_by_sitecode` field (a static dict keyed by the real `SITECODE` value found in `detail`, resolved in `parcels.py` the same way `document_url_by_commune` is) — same "known instance" scope limit as flood zones: covers only the sites verified to intersect our two communes, not all ~67 real Natura 2000 sites nationally. Verified live: 7 of the 8 sites resolved against a real intersecting parcel found by hand (including one real parcel, `061E00405001802`, that genuinely intersects both a habitats AND a birds site independently). The 8th (LU0002007, "Vallée supérieure de l'Alzette") is correctly ingested and configured, but no real Luxembourg City parcel intersecting it could be found within reasonable effort — its real extent within our ingested data may be genuinely marginal (the same kind of partial-coverage reality already documented for PAG zones), reported honestly as unconfirmed rather than claimed as verified.
- [M2.3/M2.4] National legislation corpus (brief §3.3): ingested 9 real laws via the existing Legilux extractor, reused unchanged. Found that consolidated "version consolidée" and "texte coordonné" (`tc/...`) Legilux document shapes use the exact same `richtext_body`/`richtext_article` structure as the single-act RGDs already ingested — verified live on an 84-article and a 117-article real consolidated law before trusting it generically. Fixed a real title-extraction bug this surfaced: a consolidated law's `<h1>` nests its real short title as a first `<p>`, followed by a real (but irrelevant for display) table of amending acts — `extract_legilux_document` previously took the whole `<h1>`'s text, running the two together with no separator; fixed to take only the first `<p>`, verified backward-compatible with every document already ingested (single-act RGDs have only the one `<p>`). Where a real, dated consolidated snapshot could be found and verified (4 of 9 laws), tagged `LegalStatus.in_force`; where only the original "jo" text was found within the time available (5 of 9), tagged `LegalStatus.unknown` rather than asserted as current — an honest, deliberate scope limit (a full SPARQL-based amendment-chain resolution is real further work, not attempted today). One entry (patrimoine culturel) cites a real law dated 25 February 2022, not the brief's stated 8 September 2023 — searched properly and could not find a real law at that exact date on this subject; flagged here rather than silently substituted or fabricated.
- [M2.1] Built the required "status table or dashboard" (`GET /api/v1/sources/status`) as a thin read-only projection of the real `sources` table — every field it needs (`last_fetch_at`, `last_success_at`, `last_status`, `documents_ingested`, `failure_count`) has been tracked by every ingestion script since the M1 foundation (the model's own docstring already said "Drives incremental fetch + the status dashboard"); this was a real, already-designed-for gap, not new tracking infrastructure.
- [M2.4] Commune registry: found that `communes` (LAU code/name/canton/district, ingested since M1 from `Limites administratives`) already covers most of what M2.4 asks for. Added the missing fields (`website_url`, `geoportal_slug`, `population`, `cms_hosting_provider`) via migration. Real official website URLs for all 100 real communes (the brief says 102; the actual current count after Luxembourg's commune mergers is 100 — verified against our own already-ingested `Limites administratives` data, not the brief's possibly-outdated figure) were sourced by scraping SYVICOL's own real commune directory (`syvicol.lu/fr/annuaires/annuaire-des-communes`) — each commune's real page has a `Site: <a href=...>` element, not guessed from a `www.<name>.lu` pattern (several real communes don't follow it, e.g. Sanem's real site is `suessem.lu`, Vallée de l'Ernz's is `aerenzdall.lu`). Caught and fixed a real matching bug: 1 of 100 communes failed to match on the first run because SYVICOL's real markup has an unescaped HTML entity (`VALLEE DE L&#39;ERNZ`) in its `<h1>`; fixed the normalization to unescape HTML entities, verified idempotent (a second run matched 100/100 from cache alone, zero new network calls). `population` and `cms_hosting_provider` intentionally left null for most communes — no free structured population dataset was found within the time available (STATEC's real dataset is only exposed via an interactive LUSTAT interface, not a flat downloadable file), and CMS/hosting was only investigated for the real sample used for SCALING.md (33 communes), not fabricated for the other 67.
- [M2.4] SCALING.md: real, measured findings from 33 real communes (not a guess) — HTTP headers, HTML `generator` meta tags, and (where those were silent) `/wp-content/`/`/wp-includes/` path markers and real "Powered by" footer credits. Found genuine shared-platform clusters: ~42% run WordPress (a third of those on the same host, o2switch), ~12% run a distinct real Luxembourg-specific platform ("Quilium", confirmed via its own real footer credit on multiple real sites), with Drupal/TYPO3/custom CMS covering the rest of the required-8 and a handful of the sample. This directly and substantively answers the brief's own "research lead" prompt about whether communes share infrastructure.
- [M3.1, scope] Built a minimal, real lexical retrieval function (`app/services/retrieval.py::search_chunks`) — not full M3, but enough to make M3.4's EVAL.md report real measured numbers instead of being left empty or fabricated. Discovered `chunks.tsv` (Postgres full-text, `to_tsvector('simple', text)`) has been a generated, GIN-indexed column populated automatically since the M1/M2 schema was first designed — zero extra ingestion work needed for lexical search. Dense/hybrid retrieval (`chunks.embedding`, pgvector + HNSW index) is part of the same schema but was never populated — no embedding API key is configured in this environment (no `.env` file) — reported as an honest scope gap, not hidden.
- [BUG, retrieval] Building the eval surfaced two real, non-obvious full-text search bugs, not just a poor score to report honestly: (1) `plainto_tsquery` ANDs every query word together — a natural-language question shares almost no exact vocabulary with its formal-legal-text answer, so this returned zero results for nearly every real golden-set question on the first run (measured: 3% document precision). Fixed by tokenizing the query and OR-ing the terms with `to_tsquery`, letting `ts_rank_cd` score partial matches instead of rejecting anything short of a full match. (2) That fix alone let common French function words dominate ranking, since the 'simple' text-search config (deliberately chosen over 'french' to avoid stemming legal terms/article numbers — see the `Chunk` model's own comment) does not filter stopwords either; added a small hand-picked French stopword list. (3) `ts_rank_cd` without a normalization option doesn't account for document length — PAG documents are ingested as one giant chunk per document (`ingestion/pag_document_extraction.py`), unlike Legilux's per-article chunks, so an enormous, largely-irrelevant PAG chunk could outrank a short, genuinely relevant article purely by containing more raw text; fixed with `ts_rank_cd(..., 2)` (rank ÷ document length). After all three fixes: 87% document-level precision, 68% article-level accuracy on the real 32-question golden set (see EVAL.md) — each bug was caught by actually running the eval against real data and questioning a suspiciously bad result, not by assuming the first implementation was correct.
- [M2.4] Deep-ingested the remaining 6 of 8 brief-named communes (Esch-sur-Alzette, Differdange, Dudelange, Schengen, Junglinster, Sanem) for parcels/buildings/addresses and PAG/PAP zoning — the existing pipelines were already fully generic (keyed by admin_commune_code, not hardcoded to Wiltz/Luxembourg City), so this was mostly a config change (`TARGET_ADMIN_COMMUNES`, `PAG_ZIP_URLS`) plus real per-commune research (finding each commune's real PAG ZIP on data.public.lu via its stable `/api/1/datasets/<slug>/` endpoint, not guessed). The axis-order self-detection built for the original 2 communes worked correctly, unmodified, across all 6 new ones (mixed true/false per commune, exactly the empirical check it was designed for). Two real bugs surfaced by actually running this at 8-commune scale, neither hypothetical: (1) a genuine duplicate natural key in Sanem's own PCN shapefile (two distinct `ID_PARCELL` rows sharing the same commune+section+numero — confirmed via an empty pre-existing set for that commune, not a re-run artifact) — fixed by deduplicating within a batch (first occurrence kept, rest logged), matching M2.1's "log and skip, never abort" principle at the row level; (2) our own previously-working Luxembourg City PAG URL 404'd mid-session (data.public.lu's dated snapshot URLs expire when the file is republished — already documented for PCN/BD-Adresses, now confirmed to recur for PAG too) — refreshed via the same stable API, chose NOT to switch to the documented `r/<uuid>` redirect pattern this time since a live check found HEAD+Range against it doesn't confirm true Range support the way it does against the resolved dated URL (genuinely untested under time pressure, not confirmed broken either way).
- [M2.4] Building bylaws (règlement sur les bâtisses) ingested for all 8 communes — a category that turned out to be missing for Wiltz/Luxembourg City too, not just the 6 new ones (checked before assuming). Every real PDF URL was found by hand per-commune (the brief's own words: "no central repository exists — commune website only") and verified to fetch and extract real text before being added. Built `ingestion/pdf_extraction.py` (pypdf, no OCR — every real bylaw checked has real embedded text, none scanned) with real per-article splitting where the PDF's layout allows it. Two genuine, live-confirmed PDF-layout failure modes, not hypotheticals: (1) some communes' PDF export tool separates "Art. N" labels from body text in a way linear extraction never reunites (Luxembourg City, Wiltz, Schengen, Junglinster — "Art" appears literally zero times in the extracted text despite the document visibly having articles); (2) a real body paragraph cross-referencing another article ("conformément à l'article 37") can start a new line exactly where the split regex looks, over-splitting mid-paragraph (one real article number appeared 13 times in one document before this was caught). Both handled the same way: validate the split (a >30% duplicate-ref rate is treated as "not trustworthy"), fall back to one honest whole-document chunk rather than either fabricating structure or losing the document — same precedent as `ingestion/pag_document_extraction.py`'s own whole-document chunks. Result: 4 of 8 communes split cleanly per-article (Esch 92 distinct articles, Differdange 61, Dudelange 110, Sanem 97), 4 fall back to whole-document (Luxembourg, Wiltz, Schengen, Junglinster) — reported exactly as measured, not smoothed over.
- [M2, test hygiene] Re-running `make ingest-parcels`/`make ingest-pag-zones` for 8 communes surfaced two real, pre-existing test fragilities, neither related to the code being tested: `test_every_ingested_parcel_admin_commune_is_a_target_commune` hardcoded the original 2-commune allowlist (a straightforward update); `test_building_spans_multiple_parcels_with_real_overlap` hardcoded a specific `building_id` UUID — buildings get a fresh random UUID on every `make ingest-parcels` run (unlike `cadastral_id`, a real stable natural key from the source data), so that hardcoded ID went stale the moment the pipeline was re-run for more communes, even though the SAME real building still exists under a new ID. Fixed by finding the real multi-parcel building dynamically each run (`GROUP BY building_id HAVING count(*) >= 2`) rather than hardcoding an ephemeral identifier.
- [M3.1, real EVAL.md update] Re-ran the retrieval eval after the 8-commune expansion: document precision dropped from 87%→81%, article precision from 68%→58%. This is an honest, expected consequence of a much larger corpus (8x more communes' PAG/PAP/bylaw content now competes for the same golden-set queries), not a regression in the retrieval code — spot-checked the new misses and found real semantic overlap (e.g., a building bylaw's own public-notice procedure article genuinely shares vocabulary with the établissements classés law's equivalent article, and this lexical-only system can't disambiguate that without reranking). Reported as measured, not tuned to look better.
- [M2.4] Commune population: found STATEC's real dataset is exposed through LUSTAT's SDMX REST API (`lustat.statec.lu/rest/data/LU1,DF_X021/all`, dataflow "Population par canton et commune", CSV via `Accept: application/vnd.sdmx.data+csv;urn=true;file=true;labels=both`), not a flat file as the earlier pass concluded — real, annually-updated series (1821-present) keyed by the same LAU2 code already used throughout this project. 100/100 real communes matched (`ingestion/ingest_commune_population.py`); Luxembourg City resolves to 137,678 (2026).
- [M2.4 bonus, SPARQL amendment-chain resolution] Found the real SPARQL endpoint at `data.legilux.public.lu/sparqlendpoint` — the human-facing `/sparql` URL only ever serves the Angular SPA shell regardless of Accept header; found by fetching the site's own JS bundle (`main.*.js`) and grepping for literal "sparql" strings, not guessed. Real JOLux predicates used: `jolux:isMemberOf` (an expression's Consolidation, or its original "jo" record, → its stable Work), `jolux:isRealizedBy` (→ the real text URL), `jolux:dateApplicability`/`dateEndApplicability` (a Consolidation's real validity window). One law (`dechets_2012`'s texte coordonné) has no `Consolidation` nodes at all — checked directly via SPARQL rather than assumed broken — its own "jo" node carries the same validity information as `jolux:dateEntryInForce`/`dateNoLongerInForce` instead; one query handles both shapes generically (OPTIONAL for every date field) rather than branching on document type. New `legislation_versions` table (migration `15e63c62a9cf`) + `ingestion/ingest_legislation_versions.py` queried all 9 already-ingested laws: 30 real version windows, all 9 laws resolved with real data, each window matched to our own ingested `Document` where the version coincides (via new `ingestion/legilux_eli.py`, deriving both "work" and "expression" ELIs from the filestore URLs already used to fetch each law, rather than hand-typing 9 more identifiers that could drift out of sync). `app/services/legislation_versions.py::get_version_in_force` + `GET /api/v1/legislation/version-at` resolve "given a law and a date, which real version was in force" — verified live against the real ACDU law (2004/07/19/n1): 2024-01-01 correctly resolves to the real `consolide/20231001` snapshot, matching the Document already ingested. Real, load-bearing finding this surfaced, not fixed here: `dechets_2012`'s own JOLux record says the exact snapshot `ingest_national_legislation.py` ingested and tagged `LegalStatus.in_force` has actually been `no-longer-in-force` since 2015-04-03 — left as-is (a retroactive tag fix deserves its own reviewed decision, not a drive-by edit inside this module), but now honestly discoverable live via this module's own resolution query rather than hidden.
- [M2.1, fix] `ingest_commune_population.py` updated `communes.population` directly without ever writing a `Source` row, unlike every other ingestion script — meant a real, successful ingestion run was invisible on the M2.1 status dashboard (`GET /api/v1/sources/status`), the opposite of what that dashboard is for. Fixed by adding the same `insert(Source)...on_conflict_do_update` pattern used everywhere else, with `documents_ingested` set to the real matched-commune count (100). Verified live: `LUSTAT/STATEC — commune population (SDMX)` now appears with `last_status: ok`, `documents_ingested: 100`. Noticed the identical gap also exists in `ingest_commune_registry.py` (website_url/geoportal_slug/cms_hosting_provider) — not fixed here since it wasn't the one flagged, but worth the same fix if this dashboard needs to be a complete picture.
- [M2.4, PAP QE graphic PDFs, scope decision] Presented the choice per standing rule before writing code: fetch+store the real graphic-part PDFs (PAP QE's map per locality, NQ's schéma-directeur maps) vs. extend structured DOCX coefficient-table parsing beyond the one verified zone (MIX_u) vs. both. User chose graphic PDFs only — bounded effort, directly satisfies the brief's own "graphic part is equally required" line, and the raw DOCX table text is already captured verbatim and full-text searchable, so structuring it further has lower marginal value right now.
- [M2.4] PAP QE/NQ graphic-part PDFs: previously just a filename reference (`NOM_FICHIER_GR`/`NOM_FICHIER_SD_GR`), never fetched — "not text-extractable in a useful way" was true but conflated with "not worth storing at all." Since these entries already live inside the same PAG ZIP already opened for GML/DOCX (via range-request `RemoteZipFile`), fetching them needed no new bulk downloads, just additional per-entry range requests. Added `graphic_document_id`/`schema_directeur_graphic_document_id` FKs (migration `246235b7d975`), a new `_get_or_create_graphic_document` in `ingestion/ingest_pag_zones.py` that saves each real PDF to `data/cache/pag_graphics/<commune_code>/` and records it as a `Document` (`document_type=pag_graphic`, no `Chunk` — a map has no useful text) with a real `storage_path`. New `GET /api/v1/documents/{id}/file` serves the stored file directly (`FileResponse`), the first real use of the `Document.storage_path` field that had existed unused since the M1 schema design. Verified live end-to-end: a real Schengen PAP QE zone (parcel `097D00240002285`) resolves to `113_QE_Schengen.pdf`, a real 19MB map, downloadable via the API and opening as a genuine PDF. Result across all 8 communes: 177 real graphic PDFs fetched and stored (1.1GB total, correctly gitignored under `data/`), 4,455 PAP QE zones + 155 PAP NQ zones now linked to a real, servable document instead of a bare filename.
- [M4.1] Built `GET /api/v1/parcel/{cadastral_id}/report` matching the brief's exact JSON schema (`app/schemas/report.py`), assembled in `app/services/report.py` by reshaping data that already exists (`get_parcel_detail`'s addresses/constraints/PAG-PAP zoning) rather than recomputing anything — one extra batched `Document` query fills in `type`/`date`/`legal_status` that `DocumentReference` doesn't carry. Honest design choices, not fabricated fill-ins: `building_parameters` maps COS_max/DL_max to `max_footprint_ratio`/`max_density` (real GIS attributes, confidence "medium") only for parcels in a PAP Nouveau Quartier zone; height/storeys/setbacks are `null`/`"not_extracted"` everywhere, since no reliable source exists anywhere in this corpus for them (matches the brief's own explicit instruction, Section 4.1). `required_authorisations` is honestly `[]` with an `open_questions` note — M5 doesn't exist yet, so a guessed baseline would be worse than an admitted gap. `data_freshness` reuses M2.1's real `sources.last_success_at` tracking (per document_type actually cited in this report), not a single blanket timestamp.
- [M4.1, real data quirk] `zoning.pag_zone_label` is derived from the real written-part document's own opening text ("Art. N Zone <label> [<code>]") rather than a second static code→label lookup that could drift. Live-verified against a real Schengen parcel that this delimiter isn't universal: most communes use square brackets (`[MIX-u]`), Schengen uses parentheses (`(MIX–v)`), one document uses guillemets around different wording entirely — regex broadened to accept both `[` and `(`, guillemet cases fall back to the raw zone code (an honest, known gap, not silently wrong). Also found a real Luxembourg City parcel (`101A00986003628`) that genuinely intersects both a PAP QE zone AND a PAP NQ zone simultaneously — the brief's flat `pap_type` field can only report one (PAP QE shown, since it's the more common/existing-context case), so this is now disclosed via `open_questions` rather than silently dropping the NQ zone's real coefficients (which still populate `building_parameters` independently of which `pap_type` is shown).
- [M4, PDF library choice] Presented ReportLab/WeasyPrint/Typst per standing rule before writing code. Chose WeasyPrint (HTML+CSS via Jinja2) — fastest path to a genuinely professional A4 layout (headers/footers via `@page`, tables, page breaks) reusing skills already in this stack, over ReportLab's verbose manual-positioning API and Typst's added non-Python binary dependency. Needed real system libraries (Pango/cairo/gdk-pixbuf) beyond pip — not present on the dev machine, installed via Homebrew (reversible, standard packages) after confirming with the user; the eventual Docker image will need the equivalent `apt-get install libpango-1.0-0 libcairo2 libgdk-pixbuf2.0-0` (not yet built — no backend Dockerfile exists yet, a separate, not-yet-done packaging task).
- [M4.1] Built `GET /api/v1/parcel/{cadastral_id}/report` matching the brief's exact JSON schema (`app/schemas/report.py`), assembled in `app/services/report.py` by reshaping data that already exists (`get_parcel_detail`'s addresses/constraints/PAG-PAP zoning) rather than recomputing anything — one extra batched `Document` query fills in `type`/`date`/`legal_status` that `DocumentReference` doesn't carry. Honest design choices, not fabricated fill-ins: `building_parameters` maps COS_max/DL_max to `max_footprint_ratio`/`max_density` (real GIS attributes, confidence "medium") only for parcels in a PAP Nouveau Quartier zone; height/storeys/setbacks are `null`/`"not_extracted"` everywhere, since no reliable source exists anywhere in this corpus for them (matches the brief's own explicit instruction, Section 4.1). `required_authorisations` is honestly `[]` with an `open_questions` note — M5 doesn't exist yet, so a guessed baseline would be worse than an admitted gap. `data_freshness` reuses M2.1's real `sources.last_success_at` tracking (per document_type actually cited in this report), not a single blanket timestamp.
- [M4.1, real data quirk] `zoning.pag_zone_label` is derived from the real written-part document's own opening text ("Art. N Zone <label> [<code>]") rather than a second static code→label lookup that could drift. Live-verified against a real Schengen parcel that this delimiter isn't universal: most communes use square brackets (`[MIX-u]`), Schengen uses parentheses (`(MIX–v)`), one document uses guillemets around different wording entirely — regex broadened to accept both `[` and `(`, guillemet cases fall back to the raw zone code (an honest, known gap, not silently wrong). Also found a real Luxembourg City parcel (`101A00986003628`) that genuinely intersects both a PAP QE zone AND a PAP NQ zone simultaneously — the brief's flat `pap_type` field can only report one (PAP QE shown, since it's the more common/existing-context case), so this is now disclosed via `open_questions` rather than silently dropping the NQ zone's real coefficients (which still populate `building_parameters` independently of which `pap_type` is shown).
- [M4.2, design decision] Presented 3 options for generating the cover page's required "map extract showing the parcel highlighted in context" (a real WMS basemap + drawn outline vs. a self-contained vector rendering vs. skipping it). User chose the real-WMS approach — reuses the exact same public basemap WMS (`wms.geoportail.lu/opendata/service`, layer `Basemap`) already verified for the frontend map, fetched via a real `GetMap` request for the parcel's own padded bounding box (reprojected LUREF→EPSG:3857, the WMS's native CRS), with the parcel's real boundary drawn on top via Pillow (`app/services/report_map.py`) after reprojecting its geometry into the fetched image's pixel space. Cached to disk per parcel (`data/cache/parcel_maps/`, same discipline as `ingestion/download_cache.py`) — without this, every PDF request (and every test run) would re-hit a public government WMS, against the project's own "cache aggressively" standing rule.
- [M4.2, determinism] The brief requires both a "generation timestamp" in the PDF footer on every page AND a byte-comparable PDF for "the same parcel and the same corpus state" — a literal wall-clock render timestamp would break the second requirement the moment the PDF is regenerated later with unchanged data. Resolved by using the real `data_freshness.newest_source_last_success_at` (M2.1's own per-source tracking) as the footer's "Data as of" value instead of `datetime.now()` — this only changes when the underlying ingested data actually changes, which is what "corpus state" means. Verified live: two separate `report.pdf` requests for the same parcel, several seconds apart, produced byte-identical output. The one real exception to full determinism, documented rather than hidden: the embedded map-extract image is a real government WMS tile fetched at render time — stable in practice but not something this project controls bit-for-bit forever.
- [M4.3] Found the real PAG-Géoportail baseline report via a research agent rather than guessing at its content: its real name is "Rapport — Règles urbanistiques applicables à un terrain donné" (map.geoportail.lu's PAG theme), confirmed to be an async, email-delivered PDF (FME-generated, uploaded to ownCloud, link emailed) by reading the actual current `geoportailv3` open-source project (last updated 2026-09-08), not by assumption. The government's own 2015 example PDF (commune of Nommern) 404s on their live site now — recovered via the Internet Archive instead of treating "can't find it" as a dead end. Wrote the brief's required point-by-point README comparison honestly: their report is deeper on full per-article legal text (with individually clipped zone maps) and dimensional building rules (setbacks/roof forms/garages, diagram-illustrated, from the real règlement sur les bâtisses) and includes glossaries + a full legal disclaimer page — real, acknowledged gaps, not hidden. This platform is ahead on delivery speed (synchronous vs. email), machine-readable structured output (JSON alongside the PDF), explicit per-field confidence flagging (their report presents everything as flat fact), verified determinism, and overlay-constraint breadth (21 real layers vs. PAG/PAP-only in the sample studied).
